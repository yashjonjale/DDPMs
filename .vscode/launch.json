{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: DDPM Debug",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/ddpm.py",
            "console": "integratedTerminal",
            "args": [
                "--mode", "train",
                "--n_steps", "200",
                "--lbeta", "0.0001",
                "--ubeta", "0.02",
                "--epochs", "100",
                "--batch_size", "64",
                "--lr", "0.001",
                "--dataset", "moons",
                "--n_dim", "2",
                "--n_samples", "1000"
            ]
        }
    ]
}